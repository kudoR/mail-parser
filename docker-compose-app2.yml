version: '2'
services:
  fetcher:
    build: .
    environment:
    - "SPRING_PROFILES_ACTIVE=production,app1"
    - "HOST=imap.gmx.net"
    - "PORT=993"
    - "USER=${USER}"
    - "PW=${PW}"
    ports:
    - 8081:8081